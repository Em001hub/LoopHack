# Deployment Guide

## Prerequisites
- Docker & Docker Compose
- Node.js (for local dev)
- Python (for local dev)

## Steps
1. Clone the repository
2. generic .env configuration
3. Run `docker-compose up --build`
